import { View, Text, Button, StyleSheet } from 'react-native';

export default function About({navigation}){

    return(
        <View>
            <Text>about...</Text>
        </View>
    )
}